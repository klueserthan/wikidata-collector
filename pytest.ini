[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    iterator: marks tests for iterator-based APIs (select with '-m iterator')
addopts = 
    --strict-markers
    --tb=short
    -ra

