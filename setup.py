# This file is maintained for backward compatibility.
# Modern Python projects should use pyproject.toml instead.
# The build metadata is now defined in pyproject.toml.

from setuptools import setup

setup()
